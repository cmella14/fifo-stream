'use strict';

/**
 * Static stream configuration — loaded at startup.
 *
 * Streams defined here are permanent and cannot be deleted via API.
 * For dynamic streams, use the REST API (POST /api/streams).
 *
 * Leave this array empty if all streams are managed via API.
 */
module.exports = [];
