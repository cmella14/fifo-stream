# Cloudflare RTMPS ingest URL (default: live.cloudflare.com)
CF_RTMPS_URL=rtmps://live.cloudflare.com/live

# Health/API server port
PORT=3000

# Reconnect probe interval in ms (default: 5000)
RECONNECT_INTERVAL_MS=5000

# Log level: debug | info | warn | error (default: info)
LOG_LEVEL=info
